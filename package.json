{
  "name": "agent-breach-replay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/trace-schema",
    "packages/detectors",
    "packages/trace-sdk-ts",
    "apps/studio"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm --workspace @agent-breach/studio run dev",
    "studio": "npm --workspace @agent-breach/studio run dev"
  },
  "engines": {
    "node": ">=20"
  }
}
